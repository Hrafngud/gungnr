## Backend Plan

Status: Backend API and DB schema are frozen during the bootstrap rework.

Rules
- Do not modify backend code or database schema.
- No backend tasks are scheduled in this iteration.
- Any setup changes must live in `install.sh`, the new `gungnr` CLI, or frontend cleanup only.
